import {ContentContainer} from "./MainContent.styled.tsx";

const MainContent = () => {
    return (
        <ContentContainer>
            Main Content
        </ContentContainer>
    );
};

export default MainContent;
